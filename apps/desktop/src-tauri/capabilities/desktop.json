{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only permissions of the main window.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["notification:default"]
}
